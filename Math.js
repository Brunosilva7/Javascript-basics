
const radius = 5.6

//We don't have in javascript math formulas which already exist, we should use "math" properties. 
const area = Math.PI * Math.pow(radius, 2)

console.log(area)
console.log(typeof Math)