console.log("sentence of code")

{       //javascript is a language that use the same code blocks like C language. It use the key symbol.
    {
        console.log("Hello");
        console.log("world") // we could put some code within the others
    }
}